<div class="page-data">
            
            <form method="POST" action="/user/edit/{{user.id}}">
                <table cellpadding="11">
                <tr>
                    <td class="label">Name</td><td>: <input type="text" name="name" required value= "{{user.name}}"></td>
                 </tr>
                 <tr>
                    <td class="label">username</td><td>: <input type="text" name="username" required value="{{user.username}}"></td>
                 </tr>
                <tr>
                    <td class="label">password</td><td>: <input type="password" name="password" required value="{{user.password}}"></td>
                 </tr>
                  <tr>
                    <td class="label">email</td><td>: <input type="text" name="email" required value={{user.email}}></td>
                 </tr>
                 <tr>
                    <td class="label">location</td><td>: <input type="text" name="location" required value="{{user.location}}"></td>
                 </tr>
                 <tr>
                    <td class="label"></td>
                    <td>
                        &nbsp; <input type="submit" name="submit" value="Save">
                       <input type="button" name="cancel" value="Cancel" class="cancel" onClick="cancelAdd()">
                    </td>
                 </tr>
                </table>
            </form>
            
        </div>